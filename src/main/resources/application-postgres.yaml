spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/appointments}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:appointments}
    password: ${DB_PASSWORD}
